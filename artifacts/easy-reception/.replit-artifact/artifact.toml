kind = "web"
previewPath = "/"
title = "Easy Reception AI"
version = "1.0.0"
id = "artifacts/easy-reception"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26218

[services.development]
run = "pnpm --filter @workspace/easy-reception run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/easy-reception", "run", "build" ]
publicDir = "artifacts/easy-reception/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26218"
BASE_PATH = "/"
