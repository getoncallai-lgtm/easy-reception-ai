{
  "buildCommand": "pnpm --filter @workspace/easy-reception run build",
  "outputDirectory": "artifacts/easy-reception/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
